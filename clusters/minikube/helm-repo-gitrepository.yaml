apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: helm-repo
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/YOUR_GITHUB_USER/REPO_NAME
  ref:
    branch: main
  secretRef:
    name: flux-system
